[diff]
    tool = "nvimdiff"
[difftool]
    prompt = false
[difftool "nvimdiff"]
    cmd = nvim -d "$LOCAL" "$REMOTE"
[merge]
	tool = "nvimdiff"
[mergetool]
	prompt = false
[mergetool "nvimdiff"]
	cmd = nvim -d -c "wincmd l" -c "norm ]c" "$LOCAL" "$REMOTE" "$MERGED"
